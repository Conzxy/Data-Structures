#include "List.h"

using namespace std;

int main()
{
    List L;
    //BubbleSort(L);
    //BubbleSort_each(L);
    //BubbleSort_flag(L);

    //SelectSort(L);

    //InsertSort(L);
    //BinInsertSort(L);

    //ShellSort(L);

    //QuickSort(L,0,L.length()-1);

    //BucketSort(L);

    //CountSort(L);

    //RadixSort(L);

    //MergeSort_top_to_bottom(L,0,L.length()-1);//error!
    //MergeSort_bottom_to_top(L,L.length());

    //HeapSort(L);

    for (int i = 0; i < L.length(); i++)
         cout << L[i]<<" ";

}
